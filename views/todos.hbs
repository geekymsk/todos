<ul>
  {{#each todos}}
    <li>
        <!-- <input type="checkbox">
        <span>{{this}}</span> -->
        <input type="checkbox" {{#if this.done}}checked{{/if}}>
        <span class="{{#if this.done}}checked{{/if}}">{{this.text}}</span>
    </li>
  {{/each}}
</ul>
